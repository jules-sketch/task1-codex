# Buddie — Roman Numeral Calculator

A single-page calculator that performs Roman numeral arithmetic using an all-blue interface.

## Running the app
Open `index.html` in any modern browser, or serve the directory locally:

```
python -m http.server 8000
```

Then visit `http://localhost:8000` and start calculating with Buddie.
